#ifndef __UTILS__
  #define __UTILS__

  #include <string>

  void helloWorld();
  void createPlayer(std::string name, int level);
  void error();
  void enumeration();
  void files();
  void nameSpace();
  void structure();
#endif // !__UTILS__
