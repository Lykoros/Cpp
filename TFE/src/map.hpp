#ifndef __MAP__
  #define __MAP__
  int map(int size);

  class Case {
    public:
      Case(const int x, const int y);
      char skin;
      float speed;
      int x;
      int y;
    private:
  };
#endif // DEBUG
